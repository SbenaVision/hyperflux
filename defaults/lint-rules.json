{
  "rules": [
    {
      "id": "no-hardcoded-copy",
      "severity_default": "warn",
      "scope": "src",
      "description": "User-facing string literals in JSX should be externalized to copy rules.",
      "patterns": [
        { "match": "string-literal-in-jsx-text",      "exclude": ["test", "spec", "story", "stories", ".d.ts"] },
        { "match": "string-literal-in-jsx-attribute",  "exclude": ["test", "spec", "story", "stories", ".d.ts"] }
      ],
      "suggestion": "Replace with: useRule('copy.<domain>.<key>')",
      "fixable": true
    },
    {
      "id": "no-magic-numbers",
      "severity_default": "warn",
      "scope": "src",
      "description": "Numeric literals in conditional expressions should be config rules.",
      "patterns": [
        { "match": "number-literal-in-binary-comparison", "exclude": ["test", "spec", "story", "stories", ".d.ts"] }
      ],
      "suggestion": "Replace with: useRule('config.<domain>.<key>')",
      "fixable": true
    },
    {
      "id": "no-inline-config",
      "severity_default": "warn",
      "scope": "src",
      "description": "Inline object literals as JSX props suggest configuration that should be a config rule.",
      "patterns": [
        { "match": "object-literal-as-jsx-prop", "exclude": ["test", "spec", "story", "stories", ".d.ts"] }
      ],
      "suggestion": "Replace with: useRule('config.<domain>.<key>')",
      "fixable": false
    },
    {
      "id": "rules-no-cycles",
      "severity_default": "error",
      "scope": "rules",
      "description": "Rule dependency cycles are not allowed.",
      "fixable": false
    },
    {
      "id": "rules-no-orphans",
      "severity_default": "warn",
      "scope": "rules",
      "description": "Rules that are never referenced by other rules may be dead code.",
      "fixable": false
    },
    {
      "id": "rules-domain-match",
      "severity_default": "error",
      "scope": "rules",
      "description": "Every rule path must start with its domain name.",
      "fixable": false
    },
    {
      "id": "hiflux-no-legacy-refs",
      "severity_default": "error",
      "scope": "rules",
      "description": "HiFlux rules (hiflux.*) must not depend on legacy admin namespaces (config.*, copy.*, pricing.*, users.*, admin.*).",
      "fixable": false
    },
    {
      "id": "hiflux-no-admin-import",
      "severity_default": "error",
      "scope": "src",
      "description": "HiFlux source files must not import from benchmark/ or rules/admin.json.",
      "fixable": false
    },
    {
      "id": "hiflux-content-in-rules",
      "severity_default": "error",
      "scope": "rules",
      "description": "Content-shaped entries (label, title, button, placeholder, message, header) must live in content/, not rules/.",
      "fixable": false
    }
  ],
  "defaults": {
    "src_globs": ["src/**/*.ts", "src/**/*.tsx"],
    "rule_globs": ["rules/**/*.json"],
    "ignore": []
  }
}
